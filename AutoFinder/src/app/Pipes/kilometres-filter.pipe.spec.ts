import { KilometresFilterPipe } from './kilometres-filter.pipe';

describe('KilometresFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new KilometresFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
