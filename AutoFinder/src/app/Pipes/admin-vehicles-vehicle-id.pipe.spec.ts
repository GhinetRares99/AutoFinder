import { AdminVehiclesVehicleIDPipe } from './admin-vehicles-vehicle-id.pipe';

describe('AdminVehiclesVehicleIDPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminVehiclesVehicleIDPipe();
    expect(pipe).toBeTruthy();
  });
});
