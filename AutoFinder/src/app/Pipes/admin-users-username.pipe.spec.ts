import { AdminUsersUsernamePipe } from './admin-users-username.pipe';

describe('AdminUsersUsernamePipe', () => {
  it('create an instance', () => {
    const pipe = new AdminUsersUsernamePipe();
    expect(pipe).toBeTruthy();
  });
});
