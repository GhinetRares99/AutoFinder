import { AdminEventsVehicleIDPipe } from './admin-events-vehicle-id.pipe';

describe('AdminEventsVehicleIDPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminEventsVehicleIDPipe();
    expect(pipe).toBeTruthy();
  });
});
