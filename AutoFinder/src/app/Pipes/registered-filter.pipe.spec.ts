import { RegisteredFilterPipe } from './registered-filter.pipe';

describe('RegisteredFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RegisteredFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
