import { FabricationYearFilterPipe } from './fabrication-year-filter.pipe';

describe('FabricationYearFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FabricationYearFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
