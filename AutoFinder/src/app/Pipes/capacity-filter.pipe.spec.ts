import { CapacityFilterPipe } from './capacity-filter.pipe';

describe('CapacityFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CapacityFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
