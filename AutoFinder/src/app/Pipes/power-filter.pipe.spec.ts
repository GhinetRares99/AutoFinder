import { PowerFilterPipe } from './power-filter.pipe';

describe('PowerFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PowerFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
