import { FirstRegistrationFilterPipe } from './first-registration-filter.pipe';

describe('FirstRegistrationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FirstRegistrationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
