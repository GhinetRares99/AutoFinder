import { TransmissionFilterPipe } from './transmission-filter.pipe';

describe('TransmissionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TransmissionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
