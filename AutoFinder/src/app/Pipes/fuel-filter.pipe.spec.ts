import { FuelFilterPipe } from './fuel-filter.pipe';

describe('FuelFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FuelFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
