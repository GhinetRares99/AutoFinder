import { DoorsFilterPipe } from './doors-filter.pipe';

describe('DoorsFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DoorsFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
