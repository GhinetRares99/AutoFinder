import { AdminVehiclesUserIDPipe } from './admin-vehicles-user-id.pipe';

describe('AdminVehiclesUserIDPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminVehiclesUserIDPipe();
    expect(pipe).toBeTruthy();
  });
});
