import { PollutionNormFilterPipe } from './pollution-norm-filter.pipe';

describe('PollutionNormFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PollutionNormFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
