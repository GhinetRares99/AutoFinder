import { AdminUsersUserIDPipe } from './admin-users-user-id.pipe';

describe('AdminUsersUserIDPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminUsersUserIDPipe();
    expect(pipe).toBeTruthy();
  });
});
