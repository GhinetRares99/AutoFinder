import { WarrantyFilterPipe } from './warranty-filter.pipe';

describe('WarrantyFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new WarrantyFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
