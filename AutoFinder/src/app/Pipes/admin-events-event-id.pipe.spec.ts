import { AdminEventsEventIDPipe } from './admin-events-event-id.pipe';

describe('AdminEventsEventIDPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminEventsEventIDPipe();
    expect(pipe).toBeTruthy();
  });
});
